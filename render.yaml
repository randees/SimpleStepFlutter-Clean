services:
  - type: web
    name: simple-step-flutter
    env: node
    buildCommand: |
      npm install
    startCommand: node simple-server.js
    # Note: Environment variables must be set in Render dashboard
    # The simple-server.js will serve both static files and provide /api/config endpoint
