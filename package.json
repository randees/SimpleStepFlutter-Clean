{
  "name": "simple-step-flutter-server",
  "version": "1.0.0",
  "description": "Static file server for Flutter web app",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "echo 'Using pre-built Flutter files'"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
